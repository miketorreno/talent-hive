[tool.poetry]
name = "domain"
version = "0.1.0"
description = "Pure domain entities and state machines for Talent Hive"
authors = ["Mike Torreno"]

[tool.poetry.dependencies]
python = "^3.12"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
